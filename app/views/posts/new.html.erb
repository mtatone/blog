<div id="page_wrapper">
  <h1>New Post</h1>
  <% @post.errors.messages.each do |name , message| %>
  <% require 'pry'; binding.pry %>
    <%= content_tag(:div, name.to_s + message.to_s, class:"alert") %>
  <% end %>
  <%= render 'form' %>
</div>